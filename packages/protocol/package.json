{
  "name": "@encastra/protocol",
  "version": "0.5.0-rc.1",
  "private": true,
  "description": "Component protocol: type system, manifest schema, canonicalisation. The single source of truth shared by the editor and the runtime.",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./data/type-graph.json": "./data/type-graph.json"
  },
  "files": [
    "dist",
    "data",
    "schema",
    "wit"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  }
}
