{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The four things the editor needs from Tauri itself. Everything else the application does goes through its own commands, and through the capability broker behind them.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
